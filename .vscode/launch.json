{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CampusSim",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/CampusSim",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build with cmake",
            "MIMode": "lldb"
        }
    ]
}